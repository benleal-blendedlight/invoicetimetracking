# SharePoint list definitions

Canonical column reference lives in:

- [`docs/sharepoint-schema.md`](../../docs/sharepoint-schema.md)

Runtime provisioning:

- [`scripts/provision.ps1`](../../scripts/provision.ps1)

This folder is reserved for optional future artifacts:

- PnP provisioning XML templates
- Exported list schemas for DR
- Site scripts / site designs

Prefer editing **`docs/sharepoint-schema.md`** + **`scripts/provision.ps1`** together when the model changes.
